'use client';

const CourtForm = ({someProp}) => {

    console.log(someProp);
    
    return (
        <div className="w-full text-black">
            Суды
        </div>
    );
};

export default CourtForm;
